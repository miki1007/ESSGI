buildscript {
    ext.kotlin_version = "1.6.10" // or a compatible version for your Gradle setup
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        classpath "com.android.tools.build:gradle:7.5.0" // Match with Gradle version
    }
}


allprojects {
    repositories {
        google()
        mavenCentral()
       
    }
}
